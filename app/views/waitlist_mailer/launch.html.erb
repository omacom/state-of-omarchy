<h2 style="margin:0 0 12px">The survey is live</h2>
<p class="muted">Thanks for waiting — the <%= @survey.title %> <%= @survey.year %> survey is open now.</p>
<%= link_to "Take the survey", @url, class: "btn" %>
