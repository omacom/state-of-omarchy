<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      body { font-family: -apple-system, "Segoe UI", Helvetica, Arial, sans-serif; color: #222; }
      .wrap { max-width: 480px; margin: 0 auto; padding: 24px; }
      .muted { color: #555; }
      .fine { color: #888; font-size: 12px; }
      .btn { display: inline-block; background: #000; color: #fff !important; padding: 12px 24px; text-decoration: none; margin: 16px 0; }
      .code { font-size: 28px; font-weight: bold; letter-spacing: 8px; margin: 8px 0; font-family: ui-monospace, Menlo, monospace; }
    </style>
  </head>
  <body>
    <div class="wrap"><%= yield %></div>
  </body>
</html>
